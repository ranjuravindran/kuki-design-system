{
  "name": "@kuki/design-system",
  "version": "0.1.0",
  "description": "KuKi design system — tokens + React components (Android/iOS consume tokens/tokens.json)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "files": ["src", "styles"],
  "sideEffects": ["styles/*.css"],
  "peerDependencies": {
    "react": ">=18"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "typescript": "^5.5.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
